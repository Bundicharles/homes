# Production API URL (no trailing slash)
VITE_API_URL=https://yourdomain.com/backend/api

# Base URL for uploaded files (e.g. property images). Use '/' if served from same domain
VITE_UPLOAD_BASE=https://yourdomain.com
