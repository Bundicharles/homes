# For a real deployment, see DEPLOYMENT.md.
# VITE_API_URL and VITE_UPLOAD_BASE are intentionally unset so that
# the runtime environment detection in api.ts and utils/index.js
# picks the correct path (local /homes/ vs production /).

